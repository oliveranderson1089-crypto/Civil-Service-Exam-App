# 运行所需
flask
waitress
pypinyin
reportlab
pillow          # OCR 前的图像预处理（灰度 / 放大 / 去噪 / 透明底铺白）
pillow-heif     # iPhone 的 HEIC / HEIF 图片
# 仅构建时用到（生成图标 / 校验字体覆盖），运行可不装
fonttools
# 仅测试用：.venv/bin/python -m pytest tests/ -q
pytest
