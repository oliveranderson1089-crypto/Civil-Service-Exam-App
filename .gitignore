# Python 虚拟环境与缓存
.venv/
__pycache__/
*.pyc

# 运行时生成（含个人数据 / 密码，切勿上传）
app.db
# SQLite WAL/SHM：**必须一起忽略**。它们是尚未合并回主库的最近写入页，
# 内容跨全部业务表（users/notes/ai_chats…），而这个仓库是公开的 —— 提交上去等于
# 把一份用户数据快照推进公开历史。只写 app.db 的话 `git add -A` 照样会把它俩扫进来。
app.db-wal
app.db-shm
app.db-journal
config.json
# 异地备份的仓库地址和密钥（restic）。backup.env.example 是模板、可以上传，
# 填好的 backup.env 里是 R2/B2 的 access key，绝不能进公开仓库。
backup.env

# 导出/临时文件
*.pdf
*.log
.DS_Store

# 安卓构建产物（体积大，可重新生成）
android/build/
android/.gradle/
android/local.properties
*.keystore
*.idsig

# 用户上传 / 备份
uploads/
*.bak
*.bak.*

# 古诗文原始大数据(用 .gz 版)
data/classics.json
android/app/build/
desktop/build/
node_modules/
package-lock.json
uploads/realfig/

# 安装包不进仓库：二进制每发一版就在 git 历史里永久留一整份（改一个字节也是整份新对象，
# delta 压缩对已压缩的 zip/apk 基本无效）。Windows 的 Electron 包 ~135MB；安卓包也从
# 当初的 3MB 长到了 13MB，27 个历史版本正是这个仓库 .git 目前 217MB 的主要来源。
# 服务端是从**文件系统**发包的（mods/dist.py 直接读 dist/ 下的文件），不依赖 git；
# 版本清单 dist/apk.json / deb.json / win.json 是几百字节的文本，照旧提交——
# 应用内更新比对版本号靠的是它们，不是包本身。
dist/gongkao-win.zip
dist/gongkao-setup.exe
dist/gongkao.apk
dist/gongkao.deb
