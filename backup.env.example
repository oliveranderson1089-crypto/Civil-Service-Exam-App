# 异地备份配置 —— 复制成 backup.env 填好即可启用（backup.env 已在 .gitignore 里）。
#
#   cp backup.env.example backup.env && chmod 600 backup.env
#   # 填好之后试跑一次：
#   systemctl --user start gongkao-backup.service && journalctl --user -u gongkao-backup -n 30
#
# 填之前先想清楚一件事：**放在这台机器上的任何路径都不算异地。**
# 这台机器只有一块 nvme，本地快照和原始库在同一块盘上，盘挂了是一起没的。
#
# 格式注意：这个文件由 systemd 的 EnvironmentFile 读，只认 KEY=value，
# **不要写 export**（systemd 会把 "export X" 整个当成变量名）。
# 要在自己的 shell 里用（比如手动跑 restore_drill.sh --offsite），这样加载：
#   set -a; . ./backup.env; set +a

# ---------------------------------------------------------------- 仓库地址
# 选一种，把注释去掉：

# ① Cloudflare R2（你已经在用 CF 隧道，账号是现成的；10 GB 免费额度）
#    先到 R2 面板建一个 bucket，再建一个 API Token 拿到 access key。
# GONGKAO_RESTIC_REPO=s3:https://<account-id>.r2.cloudflarestorage.com/<bucket>
# AWS_ACCESS_KEY_ID=<R2 access key id>
# AWS_SECRET_ACCESS_KEY=<R2 secret access key>

# ② Backblaze B2（10 GB 免费）
# GONGKAO_RESTIC_REPO=b2:<bucket>:gongkao
# B2_ACCOUNT_ID=<keyID>
# B2_ACCOUNT_KEY=<applicationKey>

# ③ 外接硬盘 / U 盘（零成本，但要记得插）
#    注意：盘没插上时这个路径不存在，restic 会报错、备份记 failed —— 这是**故意的**，
#    比「悄悄跳过、以为有备份」强。后台面板会显示红灯提醒你插盘。
# GONGKAO_RESTIC_REPO=/media/pei-gen-li/<盘名>/restic-gongkao

# ④ 另一台机器（走 SFTP，需要先配好免密 ssh）
# GONGKAO_RESTIC_REPO=sftp:user@192.168.3.x:/backup/gongkao

# ---------------------------------------------------------------- 仓库密码
# restic 的仓库是加密的，这把密码丢了仓库就再也打不开了。
# **务必单独抄一份到密码管理器或纸上** —— 只存在这台机器上等于没存。
#
#   head -c 32 /dev/urandom | base64 > ~/.config/gongkao-restic-pass
#   chmod 600 ~/.config/gongkao-restic-pass
#
# 这里要写绝对路径：systemd 读这个文件时不会展开 $HOME 或 ~。
# GONGKAO_RESTIC_PASSWORD_FILE=/home/pei-gen-li/.config/gongkao-restic-pass

# ---------------------------------------------------------------- 本地快照位置
# 一般不用改。同样不展开 ~，要写绝对路径。
# GONGKAO_BACKUP_DEST=/home/pei-gen-li/AppStore/backups/gongkao
